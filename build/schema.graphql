type Ink @entity {
  id: ID!
  inkId: BigInt!
  jsonUrl: String!
  inkUrl: String!
  artist: Bytes!
  limit: BigInt!
  token: Token!
}

type Token @entity {
  id: ID!
  tokenId: BigInt!
  inkUrl: String!
  owner: Bytes!
}
